query CurrentMember($source: UpsellTravelAddonSource!) {
  currentMember {
    upsellTravelAddonBanner(source: $source) {
      contractIds
      titleDisplayName
      descriptionDisplayName
      badges
    }
  }
}

import Apollo
import Flow
import Foundation
import hCore
import hGraphQL

class AddressState {
    var query: GraphQL. {
            GraphQL.QuoteBundleQuery(ids: ids, locale: Localization.Locale.currentLocale.asGraphQLLocale())
        }
}

mutation chatSendText($input: ChatMessageTextInput!) {
  chatSendText(input: $input) {
    error {
      message
    }
    message {
      ... MessageFragment
    }
    status {
      message
    }
  }
}

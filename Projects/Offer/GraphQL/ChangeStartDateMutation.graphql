mutation ChangeStartDate($id: ID!, $startDate: String!) {
    editQuote(
        input: {
            id: $id
            startDate: $startDate
        }
    ) {
        ... on CompleteQuote {
            startDate
        }
    }
}

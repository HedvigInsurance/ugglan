mutation MoveIntentCommit($intentId: ID!, $homeQuoteId: ID) {
  moveIntentCommit(intentId: $intentId, homeQuoteId: $homeQuoteId) {
    moveIntent {
        ...MoveIntentFragment
    }
    userError {
        message
    }
  }
}

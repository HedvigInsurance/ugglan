query ClaimStatusCards($locale: Locale!) {
      claims_statusCards(locale: $locale) {
        id
        pills {
          text
          type
        }
        title
        subtitle
        progressSegments{
          text
          type
        }
  }
}

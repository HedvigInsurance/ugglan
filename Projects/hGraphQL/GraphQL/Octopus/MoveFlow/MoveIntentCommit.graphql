mutation MoveIntentCommit($intentId: ID!, $homeQuoteId: ID, $removedAddons: [ID!]) {
  moveIntentCommit(intentId: $intentId, homeQuoteId: $homeQuoteId, removedAddons: $removedAddons) {
    moveIntent {
        ...MoveIntentFragment
    }
    userError {
        message
    }
  }
}


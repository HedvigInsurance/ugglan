mutation MidtermChangePetId($contractId: ID!, $petId: String!) {
  midtermChangePetId(contractId: $contractId, petId: $petId) {
    ... on MidtermChangePetIdActivationDate {
      activationDate
    }
    ... on UserError {
      message
    }
  }
}
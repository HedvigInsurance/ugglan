query CurrentMember {
  currentMember {
    email
    travelCertificateSpecifications {
      contractId
      minStartDate
      maxStartDate
      maxDurationDays
      numberOfCoInsured
      location {
        street
      }
    }
  }
}

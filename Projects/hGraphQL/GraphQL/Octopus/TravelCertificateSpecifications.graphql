query CurrentMember {
  currentMember {
    travelCertificateSpecifications {
      contractId
      minStartDate
      maxStartDate
      maxDurationDays
      numberOfCoInsured
      email
    }
  }
}

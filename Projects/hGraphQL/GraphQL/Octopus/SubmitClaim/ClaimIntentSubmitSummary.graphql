mutation ClaimIntentSubmitSummary($input: ClaimIntentSubmitSummaryInput!) {
  claimIntentSubmitSummary(input: $input) {
    intent {
      id
      currentStep {
        ...ClaimIntentStepFragment
      }
    }
    userError {
      message
    }
  }
}


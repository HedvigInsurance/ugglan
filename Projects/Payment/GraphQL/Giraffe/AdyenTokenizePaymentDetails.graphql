mutation AdyenTokenizePaymentDetails($input: ConnectPaymentInput!) {
    paymentConnection_connectPayment(input: $input) {
      ... on ConnectPaymentFinished {
        paymentTokenId
        status
      }
      ... on ActionRequired {
        paymentTokenId
        actionV2
      }
    }
}

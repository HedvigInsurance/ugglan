mutation BankIDNorwayAuth($personalNumber: String!) {
    norwegianBankIdAuth(
        personalNumber: $personalNumber
    ) {
        ... on NorwegianBankIdAuthResponse {
            redirectUrl
        }
    }
}

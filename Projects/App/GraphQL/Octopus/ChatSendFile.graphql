mutation ChatSendFile($input: ChatMessageFileInput!) {
  chatSendFile(input: $input) {
    error {
      message
    }
    message {
      ... MessageFragment
    }
    status {
      message
    }
  }
}

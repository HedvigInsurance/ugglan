query ActiveContractBundles {
     activeContractBundles {
        id
        contracts {
          id
          inception
          termination
      }
        angelStories {
          addressChange
        }
  }
}

query Translations($code: String) {
    languages(where: { code: $code }) {
        translations(where: { project: App }) {
            key {
                value
            }
            text
        }
    }
}

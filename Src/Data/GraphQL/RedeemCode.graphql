mutation RedeemCode($code: String!) {
    redeemCode(code: $code) {
        cost {
            monthlyDiscount {
                amount
            }
            monthlyGross {
                amount
            }
            monthlyNet {
                amount
            }
        }
    }
}

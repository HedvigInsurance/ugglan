query KeyGearItem($id: ID!) {
    keyGearItem(id: $id) {
      photos {
        file {
          preSignedUrl
        }
      }
      receipts {
        file {
          preSignedUrl
        }
      }
    }
}

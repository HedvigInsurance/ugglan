query Marketing($languageCode: String!) {
    appMarketingImages(
        where: {
            language: { code: $languageCode }
        }
    ) {
        image {
            url
        }
        blurhash
    }
}

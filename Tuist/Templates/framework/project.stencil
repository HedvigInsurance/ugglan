import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.framework(
    name: "{{ name }}",
    targets: Set([
       .framework,
       .tests,
       .example,
       .testing
    ]),
    dependencies: [],
    sdks: []
)

import ProjectDescription

let config = Config(
    compatibleXcodeVersions: .exact("13.3"),
    cloud: nil,
    cache: nil,
    swiftVersion: nil,
    plugins: [],
    generationOptions: .options()
)

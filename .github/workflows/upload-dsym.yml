name: Upload DSYM

on:
 push:
    branches:
      - master
 schedule:
    - cron:  '*/15 * * * *'

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Fastlane Action
      uses: maierj/fastlane-action@v1.4.0
      with:
       lane: ios refresh_dsyms
